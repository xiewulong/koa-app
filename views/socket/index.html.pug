extends ../layouts/application.html.pug

prepend title
  = `${__('views.socket.index.title')} - `

append page_stylesheets
  link(rel="stylesheet" type="text/css" href=`/assets/css/socket.css?${version}`)

append page_javascripts
  script(type="text/javascript" src=`/assets/js/socket.js?${version}`)

block content
  h1 socket/index
  p Find me in views/socket/index.html.pug

  .chat
    .items: ul
    form(name="chat")
      input(name="message" autocomplete="off" autofocus="autofocus")
      button(type="submit") Submit
