extends ../layouts/application.html.pug

block title
  = `${__('views.dashboard.index.title')} - `

block page_stylesheets
  link(rel="stylesheet" type="text/css" href=`/assets/css/dashboard.css?${version}`)

block page_javascripts
  script(type="text/javascript" src=`/assets/js/dashboard.js?${version}`)

block content
  h1 dashboard/index
  p Find me in views/dashboard/index.html.pug
  p #{__('app.words.hello')}, #{current_user.username},
    a(href="/user/logout" data-user="logout" data-method="post")= __('app.words.logout')
